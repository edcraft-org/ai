{
  "template_id": "conditionals.string_route",
  "version": 1,
  "topic": "conditionals",
  "difficulty": "intermediate",
  "code": "def route(mode):\n    if mode == \"express\":\n        return 1\n    if mode == \"standard\":\n        return 2\n    return 3",
  "entry_function": "route",
  "parameters": [
    {
      "name": "mode",
      "kind": "string",
      "values": ["express", "standard", "economy"]
    }
  ],
  "question_template": "How many if conditions are evaluated when route receives mode {mode}?",
  "answer_target": "branch_executions",
  "answer_expression": "1 if mode == \"express\" else 2",
  "distractors": [
    {
      "expression": "2 if mode == \"express\" else 1",
      "reason_template": "Reverses the early-return behavior for mode {mode}."
    },
    {
      "expression": "3",
      "reason_template": "Counts all possible paths rather than evaluated conditions."
    },
    {
      "expression": "4",
      "reason_template": "Counts return statements as additional conditions."
    }
  ],
  "question_type": "mcq"
}
