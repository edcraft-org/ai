{
  "code": [
    "def delivery_fee(weight, express):",
    "    fee = 5",
    "    if weight > 10:",
    "        fee += (weight - 10) * 2",
    "    elif weight > 5:",
    "        fee += weight - 5",
    "    if express:",
    "        fee *= 1.5",
    "    return round(fee, 2)"
  ],
  "entry_function": "delivery_fee",
  "inputs": {"weight": 12, "express": true},
  "question": "What value is returned by delivery_fee(12, True)?",
  "proposed_answer": 13.5,
  "distractors": [9.0, 11.0, 18.0],
  "question_type": "mcq"
}
